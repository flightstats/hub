<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/home/hub/log/hub.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>/home/hub/log/hub_%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <!-- keep 14 days -->
            <maxHistory>14</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%5p %d{ISO8601} [%t] %c [line %L] - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="FILE"/>
    </root>

    <logger name="com.sun.jersey.server.wadl.generators" level="WARN"/>
    <logger name="org.apache.zookeeper.server.PrepRequestProcessor" level="WARN"/>
    <logger name="com.flightstats.hub.group" level="{{ verbosity }}"/>
    <logger name="com.flightstats.hub.dao" level="{{ verbosity }}"/>
    <logger name="com.flightstats.hub.time" level="{{ verbosity }}"/>
    <logger name="com.flightstats.hub.spoke.SpokeResource" level="{{ verbosity }}"/>
    <logger name="com.flightstats.hub.spoke" level="{{ verbosity }}"/>
    <logger name="com.flightstats.hub.spoke.SpokeTtlEnforcer" level="TRACE"/>

</configuration>
