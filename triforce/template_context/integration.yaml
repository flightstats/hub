## This file holds the context data for the DEV environment to fill template files.
## The syntax is:
##
## relative/path/to/file :
##     _template: tplFile.jinja
##     parameter_name : parameter_value
##     parameter2 :
##         - item1
##         - item2
##
## The file should be in yaml syntax, with one top-level array for each template file.
## There should be a corresponding template file in triforce/tpl/ written in the Jinja templating language
## for each top-level item.
##
conf/hub.properties :
    _template : hub.properties.jinja

    app :
        name : "hub"
        environment : "int"
        encrypted : false
        shutdown_delay_seconds : 15
        shutdown_wait_seconds : 60

    backing :
        store : "aws"

    aws :
        protocol : "HTTP"

    dynamo :
        endpoint : "dynamodb.us-east-1.amazonaws.com"
        table_creation_wait_minutes : 10

    s3 :
      content_backoff_wait : 1000
      content_backoff_times : 6
      environment : "int-east1.flightstats.com"
      endpoint : "s3-external-1.amazonaws.com"

    audit :
      threads: 0
      queue: 0

    graphite :
        enable: true
        registerJvmMetrics : false
        host : "svcsmon.cloud-east.staging"
        port : 2003
        prefix : hub-int
        rateSeconds : 60

    hosted_graphite :
        enable: true
        registerJvmMetrics : false
        host : "carbon.hostedgraphite.com"
        port : 2003
        prefix : 86655285-0562-449c-874a-9ae3bc33c5f2.hub.int
        rateSeconds : 5

    zookeeper :
        connection : "hub-int-zk-01.cloud-east.staging:2181,hub-int-zk-02.cloud-east.staging:2181,hub-int-zk-03.cloud-east.staging:2181"


conf/hazelcast.conf.xml :
    _template : hazelcast.conf.xml.jinja
    cluster_hosts :
        - "hub-int-01.cloud-east.staging"
        - "hub-int-02.cloud-east.staging"
        - "hub-int-03.cloud-east.staging"

bin/start.sh :
  _template : start.sh.jinja
  max_heap : "800m"
  min_heap : "256m"
  min_new : "100m"
  environment : "int"

bin/prestop.sh :
  _template : prestop.sh.jinja

conf/logback.xml :
    _template : logback.xml.jinja

    verbosity : "INFO"

lib/newrelic.yml :
  _template : newrelic.yml.jinja
